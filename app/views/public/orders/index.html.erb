<h1>注文履歴一覧</h1>
<% @orders.each do |order| %>
<p>注文日 : <%= order.created_at.strftime('%Y/%m/%d') %></p>
<p>配送先 : <%= order.address %></p>
<p>注文商品 : <% order.order_details.each do |order_detail| %>
              <%= order_detail.item.name %>
              <% end %></p>
<p>支払金額 : <%= (order.postage + order.total_payment).to_s(:delimited) %></p>
<p>ステータス : <%= order.status_i18n %>
<p>注文詳細 : <%= link_to "表示する", order_path(order.id) %></p>
<% end %>