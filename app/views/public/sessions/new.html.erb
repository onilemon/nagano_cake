<div class="container-fluid">
  <h3>ログイン</h3>
    <h4>会員の方はこちらからログイン</h4>
    <div class="row justify-content-center">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <table>
          <tr>
            <td>メールアドレス</td>
            <td><%= f.email_field :email, autofocus: true, autocomplete: "email", size: "40", class: "ml-4 mb-3" %></td>
          </tr>
          <tr>
            <td>パスワード</td>
            <td><%= f.password_field :password, autocomplete: "current-password", size: "40", class: "ml-4 mb-3" %></td>
          </tr>
          <tr>
            <td></td>
            <td><%= f.submit "ログイン" , class:"btn btn-primary ml-4" %></td>
          </tr>
        </table>
      <% end %>
    </div>
    <h4>会員登録がお済みでない方</h4>
      <%= render "public/shared/links" %>から新規登録を行なってください。
</div>
