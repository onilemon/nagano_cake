<h1>商品詳細</h1>
<div>
  <%= image_tag @item.get_image %>
  <p>商品名：<%= @item.name %></p>
  <p>商品説明：<%= @item.introduction %></p>
  <p>ジャンル：<%= @item.genre.name %></p>
  <p>税込価格 <br>
    (税抜き価格)：<%= @item.add_tax_price.to_s(:delimited) %> (<%= @item.price.to_s(:delimited) %>) 円</p>
    <% if @item.is_active? %>
    <p>販売ステータス： 販売停止中</p>
  <% else %>
    <p>販売ステータス： 販売中</p>
  <% end %>

  <%= link_to '編集する', edit_admin_item_path(@item.id), class: "btn btn-success" %>
</div>